[project]
name = "backend"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "sqlalchemy[asyncio]",
    "asyncpg",
    "alembic",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]
